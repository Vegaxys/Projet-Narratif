using PlayFab;
using PlayFab.ClientModels;
using UnityEngine;

public class Test : MonoBehaviour{

  /*  private void Start() {
        PurchaseItemRequest request = new PurchaseItemRequest();
        request.CatalogVersion = "Characters";
        request.VirtualCurrency = "VP";
        request.ItemId = "ChaiseIKEA";
        request.Price = 80;
        PlayFabClientAPI.PurchaseItem(request, result => {
            Debug.Log("GG pour l'achat de la chaise IKEA");
        }, error => {
            Debug.LogWarning(error.ErrorMessage);
        });
    }*/
    /*private void Start() {
        Alerts alert = new Alerts();
        StartCoroutine(alert.CreateNewAlert("The scene starts"));
    }*/
}
